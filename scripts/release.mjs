#!/usr/bin/env node

console.error('[release] This legacy release entry point is disabled.')
console.error('[release] Use `npm run release` or `scripts/release-allegretto.mjs` instead.')
console.error('[release] No release was published.')
process.exit(1)
